package main.model.data.credential.password_protected;
import main.model.data.credential.Credential;

public abstract class PasswordProtected extends Credential implements PasswordStrength {
    public PasswordProtected() {
        super();
    }
}
